
const Print = ({setPage, events}) => {
    return(
        <>
            {

            }
            {/* <button onClick={() => setPage("form")}>Back</button> */}
        </>
    )
}

export default Print;